<h1><%= @subject.name %></h1>
<p><%= @subject.description %></p>

<% if @subject.skills.length > 0 %>
  <%= render "skills/index", locals: {skills: @subject.skills} %>
<% else %>
  <p>Nobody's added a skill yet!</p>
<% end %>